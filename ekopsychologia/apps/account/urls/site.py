# -*- encoding: utf-8 -*-
from django.conf.urls import patterns, url
from ..views.site import UserEditListView
from ..views import site

urlpatterns = patterns('',
    #url(r'^$', site.index, name='account.index'),
)
